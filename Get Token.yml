info:
  name: Get Token
  type: http
  seq: 1

http:
  method: POST
  url: "{{gatewayURL}}/api/v1/crm/auth/login"
  body:
    type: json
    data: |-
      {
        "identifier": "admin@isp.local",
        "password": "Admin123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
